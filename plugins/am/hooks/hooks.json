{
  "description": "After a session in a project that keeps .about/, review it with the glossary and adr skills",
  "hooks": {
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if command -v bun >/dev/null 2>&1; then bun \"${CLAUDE_PLUGIN_ROOT}/hooks/session-review.ts\"; fi"
          }
        ]
      }
    ]
  }
}
